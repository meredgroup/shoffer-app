# 🚀 راهنمای راه‌اندازی کامل شوفر

## ✅ وضعیت فعلی
- ✅ فرانت‌اند در حال اجرا (http://localhost:3000)
- ⏳ بک‌اند نیاز به راه‌اندازی دارد

---

## 📋 کارهای لازم (به ترتیب)

### **مرحله ۱: نصب کتابخانه‌های بک‌اند**

```bash
# ترمینال جدید باز کنید (Terminal 2)
cd workers
npm install
```

**زمان:** ~۲-۳ دقیقه

---

### **مرحله ۲: ساخت دیتابیس محلی**

```bash
# همان ترمینال (در پوشه workers)
wrangler d1 execute shoffer-db --local --file=../schema/migrations/0001_init.sql
```

این دستور:
- ✅ ۱۸ تا جدول می‌سازد
- ✅ دیتابیس SQLite محلی راه‌اندازی می‌کند

**اگر خطا گرفتید:**
```bash
# ابتدا این را اجرا کنید
npm install -g wrangler

# سپس دوباره امتحان کنید
wrangler d1 execute shoffer-db --local --file=../schema/migrations/0001_init.sql
```

---

### **مرحله ۳: اضافه کردن داده‌های نمونه (اختیاری اما توصیه می‌شود)**

```bash
# همان ترمینال
wrangler d1 execute shoffer-db --local --file=../schema/seed.sql
```

این دستور:
- ✅ چند کاربر نمونه می‌سازد
- ✅ سفرهای نمونه اضافه می‌کند
- ✅ خودروهای نمونه ایجاد می‌کند

---

### **مرحله ۴: راه‌اندازی بک‌اند (Workers)**

```bash
# همان ترمینال (در پوشه workers)
wrangler dev
```

**موفقیت:**
```
✅ بک‌اند روی http://localhost:8787 اجرا شد
```

**تست سریع:**
- باز کنید: http://localhost:8787/health
- باید ببینید: `{"status":"healthy"}`

---

### **مرحله ۵: ساخت فایل متغیرهای محیطی (Environment Variables)**

در پوشه اصلی پروژه فایل `.env.local` بسازید:

```bash
# برگردید به پوشه اصلی
cd ..

# ایجاد فایل .env.local
```

**محتوای فایل `.env.local`:**
```env
# آدرس API بک‌اند
NEXT_PUBLIC_API_URL=http://localhost:8787

# آدرس WebSocket برای چت
NEXT_PUBLIC_WS_URL=ws://localhost:8787

# Turnstile (فعلاً نیاز نیست)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# Google OAuth (فعلاً نیاز نیست)
GOOGLE_CLIENT_ID=
```

**نکته:** اگر فایل `.env.local` وجود ندارد، فرانت‌اند به صورت پیش‌فرض از `http://localhost:8787` استفاده می‌کند.

---

### **مرحله ۶: ریستارت فرانت‌اند (اختیاری)**

اگر فایل `.env.local` ساختید:

```bash
# در ترمینال فرانت‌اند Ctrl+C بزنید
# سپس دوباره اجرا کنید
npm run dev
```

---

## ✅ چک‌لیست نهایی

بعد از انجام مراحل بالا:

- [ ] ترمینال ۱: `npm run dev` اجرا شده ✅ (قبلاً اجرا شده)
- [ ] ترمینال ۲: `wrangler dev` اجرا شده
- [ ] http://localhost:3000 باز می‌شود
- [ ] http://localhost:8787/health جواب `healthy` می‌دهد
- [ ] دیتابیس ساخته شده (۱۸ تا جدول)

---

## 🧪 **تست کامل اپلیکیشن**

### **تست ۱: ثبت‌نام**
1. برو به: http://localhost:3000
2. کلیک روی "ثبت نام"
3. انتخاب کن: "مسافر" یا "راننده"
4. فرم رو پر کن:
   - نام: علی احمدی
   - ایمیل: ali@test.com
   - رمز: 123456
5. کلیک "ثبت نام"

**انتظار:**
- ✅ اگر راننده انتخاب کردید → برید به صفحه اضافه کردن خودرو
- ✅ اگر مسافر انتخاب کردید → برید به داشبورد

---

### **تست ۲: جستجوی سفر**
1. برو به: http://localhost:3000/search
2. جستجو کن:
   - مبدا: تهران
   - مقصد: اصفهان
3. کلیک "جستجو"

**انتظار:**
- ✅ سفرهای نمونه نمایش داده می‌شود (اگر seed اجرا کردید)
- ✅ تاریخ‌ها به شمسی هستند (۱۴۰۳/۰۹/۲۲)
- ✅ اعداد فارسی هستند (۱۲۳)

---

### **تست ۳: رزرو سفر**
1. روی یک سفر کلیک کنید
2. تعداد صندلی انتخاب کنید
3. کلیک "تأیید و رزرو"

**انتظار:**
- ✅ اگر لاگین نیستید → برید به صفحه ورود
- ✅ بعد از لاگین → رزرو ثبت می‌شود
- ✅ برید به صفحه "رزروها"

---

### **تست ۴: چت (نیاز به ۲ کاربر)**

**روش ساده:**
1. یک مرورگر عادی باز کنید
2. یک مرورگر Incognito/Private باز کنید
3. در هر کدام یک کاربر مختلف لاگین کنید
4. کاربر A یک سفر رزرو کند
5. برو به `/chat` → گفتگو رو باز کن
6. پیام بفرست
7. در مرورگر دیگه ببین پیام فوری رسید یا نه

**انتظار:**
- ✅ پیام‌ها real-time نمایش داده می‌شوند
- ✅ زمان‌ها به شمسی هستند

---

### **تست ۵: ویژگی‌های راننده**

**اگر به عنوان راننده ثبت‌نام کردید:**

1. **اضافه کردن خودرو:**
   - برو به: `/drive/vehicles`
   - کلیک "افزودن خودرو جدید"
   - پر کن:
     - برند: پژو
     - مدل: ۲۰۶
     - سال: ۱۴۰۰
     - رنگ: سفید
     - پلاک: ۱۲ الف ۳۴۵ - ۶۷
     - صندلی: ۴
   - ذخیره کن

2. **ایجاد سفر:**
   - برو به: `/drive/create-ride`
   - **مرحله ۱:** خودروی اضافه شده رو انتخاب کن
   - **مرحله ۲:** 
     - مبدا: تهران
     - مقصد: اصفهان
     - تاریخ: امروز
     - ساعت: ۱۴:۰۰
   - **مرحله ۳:**
     - قیمت: ۵۰۰۰۰۰
     - صندلی: ۳
     - گزینه‌ها رو انتخاب کن
   - کلیک "ایجاد سفر"

3. **مشاهده سفرها:**
   - برو به: `/drive/my-rides`
   - سفر ایجاد شده رو ببین

**انتظار:**
- ✅ همه تاریخ‌ها شمسی
- ✅ همه اعداد فارسی
- ✅ سفر در لیست جستجو نمایش داده می‌شود

---

## 🐛 **رفع مشکلات رایج**

### **مشکل ۱: بک‌اند جواب نمی‌دهد**

**علامت:**
- در فرانت خطاهای "Failed to fetch" می‌بینید
- http://localhost:8787/health باز نمی‌شود

**راه‌حل:**
```bash
# مطمئن شوید wrangler dev اجرا شده
cd workers
wrangler dev
```

---

### **مشکل ۲: دیتابیس خالی است**

**علامت:**
- جستجو هیچ سفری نشان نمی‌دهد
- نمی‌توانید لاگین کنید

**راه‌حل:**
```bash
cd workers

# دوباره دیتابیس بسازید
wrangler d1 execute shoffer-db --local --file=../schema/migrations/0001_init.sql

# داده‌های نمونه اضافه کنید
wrangler d1 execute shoffer-db --local --file=../schema/seed.sql
```

---

### **مشکل ۳: تاریخ‌ها شمسی نیستند**

**علامت:**
- تاریخ‌ها به صورت میلادی نمایش داده می‌شوند

**راه‌حل:**
- این نباید اتفاق بیفته! همه صفحات update شدند
- اگر دیدید، Ctrl+Shift+R بزنید (hard refresh)
- Cache مرورگر رو پاک کنید

---

### **مشکل ۴: چت کار نمی‌کند**

**علامت:**
- پیام‌ها ارسال نمی‌شوند
- خطای WebSocket

**راه‌حل:**
```bash
# مطمئن شوید wrangler dev اجرا شده
cd workers
wrangler dev

# مطمئن شوید .env.local درست است
# NEXT_PUBLIC_WS_URL=ws://localhost:8787
```

---

### **مشکل ۵: "wrangler: command not found"**

**راه‌حل:**
```bash
# نصب wrangler به صورت global
npm install -g wrangler

# یا استفاده از npx
npx wrangler dev
```

---

## 📊 **وضعیت پورت‌ها**

بعد از راه‌اندازی کامل:

| سرویس | پورت | URL | وضعیت |
|--------|------|-----|--------|
| فرانت‌اند | 3000 | http://localhost:3000 | ✅ اجرا شده |
| بک‌اند | 8787 | http://localhost:8787 | ⏳ نیاز به اجرا |
| WebSocket | 8787 | ws://localhost:8787 | ⏳ نیاز به اجرا |

---

## 🎯 **دستورات خلاصه**

**راه‌اندازی کامل در ۳ مرحله:**

```bash
# مرحله ۱: نصب بک‌اند
cd workers
npm install

# مرحله ۲: ساخت دیتابیس
wrangler d1 execute shoffer-db --local --file=../schema/migrations/0001_init.sql
wrangler d1 execute shoffer-db --local --file=../schema/seed.sql

# مرحله ۳: اجرای بک‌اند
wrangler dev
```

**توقف سرویس‌ها:**
```bash
# در هر ترمینال
Ctrl + C
```

---

## 📱 **دسترسی به اپ**

بعد از راه‌اندازی کامل:

**صفحات اصلی:**
- 🏠 خانه: http://localhost:3000
- 🔍 جستجو: http://localhost:3000/search
- 📝 ثبت‌نام: http://localhost:3000/auth/register
- 🔐 ورود: http://localhost:3000/auth/login
- 👤 داشبورد: http://localhost:3000/dashboard
- 🎫 رزروها: http://localhost:3000/bookings
- 💬 چت: http://localhost:3000/chat
- 🚗 سفرهای من: http://localhost:3000/drive/my-rides

**API:**
- ✅ سلامت: http://localhost:8787/health
- 📊 تنظیمات: http://localhost:8787/config/public

---

## ✨ **نکات مهم**

1. **دو ترمینال باز داشته باشید:**
   - ترمینال ۱: `npm run dev` (فرانت)
   - ترمینال ۲: `wrangler dev` (بک‌اند)

2. **داده‌های محلی:**
   - دیتابیس محلی در `.wrangler/state` ذخیره می‌شود
   - در هر بار restart حفظ می‌شود

3. **حساب‌های کاربری seed:**
   - اگر seed اجرا کردید، می‌توانید با این اکانت‌ها لاگین کنید
   - ایمیل: test@shoffer.ir
   - رمز: shoffer123

4. **تقویم شمسی:**
   - همه تاریخ‌ها به شکل ۱۴۰۳/۰۹/۲۲
   - همه اعداد فارسی ۰۱۲۳۴۵۶۷۸۹
   - زمان‌های نسبی: "۵ دقیقه پیش"

---

## 🎊 **موفق باشید!**

حالا می‌تونید:
- ✅ ثبت‌نام کنید
- ✅ سفر جستجو کنید
- ✅ رزرو انجام بدید
- ✅ چت کنید
- ✅ سفر ایجاد کنید (راننده)
- ✅ خودرو اضافه کنید (راننده)

**اپ کامل آماده استفاده است!** 🚀

---

**تاریخ:** ۱۴۰۳/۱۰/۰۲  
**وضعیت:** ✅ آماده استفاده  
**نسخه:** 1.0.0
